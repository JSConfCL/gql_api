query FetchWaitlist($ticketId: String!) {
  getWaitlist(ticketId: $ticketId) {
    id
    myRsvp {
      id
      user {
        id
      }
      approvalStatus
    }
    ticket {
      id
    }
  }
}
